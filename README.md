This repository contains code(EDA, Analysis/Visualization Dashboard, ML models for Prediction and Classification )for Rapido dataset

Setup:

Clone the repo:
Create and activate virtual environment:
Install dependencies: Python ≥ 3.10 pandas SQLAlchemy psycopg2-binary streamlit plotly
Setup PostgreSQL database in streamlit_app.py: Update credentials - username, password and your_db
Run Pipeline: 
1. Run full EDA pipeline using python3 main.py in terminal 
2. ML models for prediction and classication problems for rapido dataset is found under ML_models folder as jupter notebooks.
2. Run streamlit_app.py using streamlit run streamlit_app.py in terminal
